{
  "name": "markdown-publish-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: publish a Markdown/Obsidian vault to GitHub Pages, then read it back via MCP.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
